docker build -f <nome_do_seu_arquivo> -t <nome_da_imagem> <caminho_para_o_contexto>
docker build -f php.dockerfile -t eliaslazcano/php:8.3 .

grep -vE '^(;|[[:space:]]*$)' php.ini-production > php.ini

docker run -d --name=webapp --restart=on-failure:3 \
    -v ${PWD}/www:/var/www/html \
    -v ${PWD}/volumes/php:/usr/local/etc/php \
    eliaslazcano/php:8.3

docker network connect geral webapp